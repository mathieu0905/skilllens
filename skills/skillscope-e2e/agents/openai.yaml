interface:
  display_name: "SkillScope E2E"
  short_description: "Run one task-skill SkillScope loop"
  default_prompt: "Run a single SkillScope end-to-end optimization loop for one selected SkillsBench task and one selected skill."
